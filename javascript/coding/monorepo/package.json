{
  "name": "tester",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "build": "yarn workspace @tester/types build && yarn install --force && tsc -b",
    "dev": "concurrently \"yarn workspace backend dev\" \"yarn workspace frontend dev\"",
    "dev:backend": "yarn workspace backend dev",
    "dev:frontend": "yarn workspace frontend dev",
    "e2e": "yarn workspace e2e test",
    "lint": "yarn workspace backend lint && yarn workspace frontend lint && yarn workspace @tester/types lint",
    "lint:fix": "yarn workspace backend lint --fix && yarn workspace frontend lint --fix && yarn workspace @tester/types lint --fix"
  }
}
